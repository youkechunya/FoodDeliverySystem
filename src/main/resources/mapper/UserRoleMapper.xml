<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserRoleMapper">
    <select id="selectRolesByUserId" resultType="com.entity.Role">
        select r.id,r.role from role r inner join user_role ur on r.id=ur.roleId where ur.userId=#{userId}
    </select>
    <insert id="insert">
        insert into user_role(userId,roleId) values(#{userId},#{roleId})
    </insert>
    <delete id="delete">
        delete from user_role where userId=#{userId} and roleId=#{roleId}
    </delete>
</mapper>